# QA bug ledger

This ledger records findings against the working tree on 2026-09-24. A source review establishes the trigger and affected path. It does not show that a focused regression test has reproduced the failure. The severity ranks expected impact, not exploitability in every deployment. Rejected candidates remain visible so later audits do not count them again.

| ID | Severity | Finding and effect | Evidence | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| QA-001 | High | A ranged download accepted `206` without checking `Content-Range`. A response for a different offset could corrupt the destination file. The data service now validates the returned start, end, and known total before exposing the body, and closes invalid responses. | `client/services/data_stream.go:78`; `client/services/data_stream.go:132` | Regression cases reject missing, malformed, wrong-unit, shifted, short, overlong, inconsistent-total, and unknown-total ranges, while accepting an exact range and a range clamped at known EOF. Invalid bodies are closed. Focused service tests and `go test ./client/transfer/... -count=1` pass; vet passes. The complete `client/services` test package remains blocked by the sandbox's loopback-listener restriction in an unrelated `httptest.NewServer` test. | Fixed |
| QA-002 | High | A download with reported size zero skipped size and SHA validation. A nonempty or corrupt response could be accepted. Metadata now distinguishes authoritative zero from unknown size, enforces known lengths, and checks any supplied SHA-256 regardless of size. | `client/transfer/types.go:20`; `client/transfer/engine/downloader.go:54`; `client/transfer/engine/downloader.go:74` | Regressions reject a nonempty body for known zero, accept a genuinely empty known-zero object, and reject wrong SHA-256 for both known-zero and unknown-size objects. Focused engine tests and `go test ./client/transfer/... -count=1` pass; vet passes. | Fixed |
| QA-003 | High | Signed download and upload transport errors previously included URL credentials in returned errors. The client now removes userinfo, query, and fragment while preserving the underlying cause and safe URL context. | `client/transfer/http.go:193`; `client/transfer/http_test.go:244` | Synthetic transport errors confirm secrets are absent, `errors.Is` is preserved, and ordinary failures retain useful context; transfer tests pass | Fixed |
| QA-004 | High | A signed LFS multipart part transport error previously reached server logs with URL credentials. LFS errors now redact userinfo, query, and fragment before returning to the logging boundary and include the failed part number. | `internal/transfers/lfs/service.go:106`; `internal/transfers/lfs/service.go:378`; `internal/httpapi/lfs_routes_test.go:327` | Synthetic route test captures logs and response, confirms no URL secrets, and retains failed-part context; LFS and HTTP API tests pass | Fixed |
| QA-005 | Medium | A download client configured not to follow redirects accepted a `3xx` response body as file data. The data service now accepts only `200` for full reads and `206` for ranged reads, while preserving the caller's redirect policy. | `client/services/data_stream.go:75`; `client/services/data_stream.go:127` | No-follow regression confirms the redirect body is rejected and closed without requesting the redirect target; the default follow policy still reaches and reads the final `200`. Focused service tests, transfer packages, direct client/download/upload importer tests, and vet pass. The complete `client/services` suite is blocked by the sandbox's loopback-listener restriction in an unrelated `httptest.NewServer` test. | Fixed |
| QA-006 | Medium | A canceled local file upload created or truncated its destination before checking the request context. Local uploads now check cancellation before filesystem changes and while reading, validate to a same-directory staging file, and replace the destination only after a complete upload. | `client/transfer/http.go:24`; `client/transfer/http.go:43`; `client/transfer/http.go:72` | Regressions confirm a pre-canceled context and cancellation during copy return `context.Canceled` without changing existing bytes. Exact-size replacement, `file:` upload, and normal umask-derived permissions pass. `go test ./client/transfer/... -count=1`, vet, and direct-importer compile checks pass. The repository-wide test run is blocked by sandbox-denied loopback listeners in unrelated tests. | Fixed |
| QA-007 | High | `decodeStrictJSON` accepted a valid JSON value followed by malformed trailing text, allowing mutation routes to accept malformed requests. It now requires the second decode to return exactly `io.EOF`. | `internal/httpapi/routes.go:122` | Before the fix, the new route regression returned 201 for a valid bucket object followed by `garbage`, and the decoder helper returned nil for malformed trailing bytes. After the fix, malformed suffixes and a second JSON value return 400 before bucket state changes; a single value and trailing whitespace pass. `go test ./internal/httpapi -count=1`, compile-only `go test ./cmd/server -run '^$' -count=1`, vet, gofmt, and diff-check pass. | Fixed |
| QA-008 | High | Create-only or update-only object access could commit a mutation and then return a read-authorization error. Registration now returns its durable service result instead of performing redundant readbacks; access-method updates return the stored DRS object without a separate read gate, matching the public DRS object endpoint. | `internal/objects/writes.go`; `internal/objects/writes_test.go` | Three SQLite regressions failed before the fix while confirming writes had committed, then passed; the duplicate registration readback is gone; object package tests, focused DRS route tests, and vet pass | Fixed |
| QA-009 | Medium | Production PostgreSQL startup retried permanent ping failures, including rejected credentials, until the startup retry window expired. It now retries connection-class SQLSTATEs, network failures, timeouts, and known schema races; authentication, authorization, missing-database, and unclassified ping errors fail fast. | `cmd/server/bootstrap.go:267`; `internal/persistence/postgres/postgres.go:84` | Before the fix, synthetic `pq.Error` values for `28P01`, `42501`, and `3D000`, plus an unknown ping failure, were all classified for retry. Afterward, the focused classifier test passes for transient and permanent cases. A live invalid-password test is included but skipped here because `SYFON_TEST_POSTGRES_DSN` is not configured. | Fixed |
| QA-010 | Medium | Non-production PostgreSQL auto-bootstrap dropped the caller context and issued schema DDL without a deadline, so cancellation or a database lock wait could leave startup stuck. The constructor and shared store now carry the caller context, every PostgreSQL bootstrap statement uses context-aware SQL, and auto-bootstrap has a 30-second default deadline that the server configures explicitly. | `internal/persistence/postgres/postgres.go:39`; `internal/persistence/postgres/postgres.go:74`; `internal/persistence/store/store.go:42`; `internal/persistence/postgres/dialect.go:73` | Before the fix, restoring background context caused the new regression to fail with `schema bootstrap received no deadline`. Caller-cancellation and bounded-DDL driver tests now pass. A live PostgreSQL table-lock test is included but skipped here because `SYFON_TEST_POSTGRES_DSN` is not configured. PostgreSQL, store, and SQLite package tests plus vet pass. The full `cmd/server` package test is blocked by sandbox-denied listeners in two unrelated lifecycle tests; compile-only checks and the `/Users/peterkor/Desktop/gen3-helm/helm/syfon` Helm config test and lint pass. | Fixed |
| QA-011 | Low | Bulk object reads previously resolved aliases with per-alias database calls. Alias IDs and distinct canonical records are now resolved in batches. | `internal/persistence/store/object_reads.go`; `internal/persistence/sqlite/sqlite_test.go` | Query-count regression reproduced 7 queries for one alias and 193 for 32 aliases before the fix; both sizes now use the same query count. Store, SQLite, and object package tests plus vet pass | Fixed |
| QA-012 | Low | Any multipart duration in seconds above `floor(MaxInt64/time.Second)` passes validation. The cleanup interval overflows and makes `time.NewTicker` panic; the inactivity timeout or completed retention can overflow and move its cutoff forward, causing premature session cleanup. | `internal/config/validation.go`; `cmd/server/bootstrap.go`; `internal/transfers/multipart.go` | `LoadConfig` rejects one-above-maximum values for all three fields and accepts the maximum; the reconciler starts with the maximum valid interval. Focused config and reconciler tests, `go vet`, the Helm configuration test, and `helm lint /Users/peterkor/Desktop/gen3-helm/helm/syfon` pass. | Fixed |
| QA-013 | Medium | A new LFS upload previously recorded usage after byte transfer and again after verify. The default OID record counted twice; a custom record ID left the transfer event under the OID. Upload usage is now recorded once at transfer completion, then reassigned to the canonical record inside pending registration before counters are flushed. | `internal/transfers/lfs/service.go`; `internal/persistence/store/object_writes.go`; `internal/transfers/lfs/verify_integration_test.go` | SQLite regressions reproduced a count of two for the default ID and an orphaned OID event for a custom ID; after the fix, both cases attribute exactly one event to the durable record, including concurrent verify and retry. LFS, store, SQLite, objects, HTTP API, and PostgreSQL package tests pass; vet passes | Fixed |
| QA-014 | High | `AuthTransport` previously added server credentials to external URLs, including after cross-host redirects. Automatic Basic and Bearer authentication is now limited to the configured server origin. | `client/request/auth.go:138`; `client/request/transport.go:208`; `client/client.go:125` | Reproduced direct and redirected leaks before the fix; focused request and client tests pass after it | Fixed |
| QA-015 | None | Internal mutation responses and bulk metadata reads include fields that a read-authorized internal route would deny, but the public DRS metadata endpoints return the same record fields. No additional disclosure was established. | `internal/objects/writes.go`; `internal/httpapi/records_routes.go`; `internal/httpapi/drs_routes.go`; `internal/httpapi/authz.go` | Route and policy review confirms public DRS single and bulk metadata reads pass no object-level read method and expose the same record fields; internal mutation routes still enforce update permission | Rejected |
| QA-016 | Medium | With no `--out`, `syfon download` trusts the record name as a path. A legacy or untrusted record name containing `../` can write outside the current directory. | `cmd/download/main.go:42`; `cmd/download/main.go:48`; `cmd/download/main.go:82` | The focused regression failed before the fix because `../nested/report.txt` remained a path; it now resolves to `report.txt`. Tests cover traversal and absolute names resolving to basenames, helper rejection of empty/dot/parent/root-only candidates, and ordinary filenames. The command still falls back to the DID when the record name is blank. Download package tests, direct importer compile, vet, and diff check pass. | Fixed |
| QA-017 | Medium | `syfon upload` and `syfon sha256sum` read an entire file or HTTP object into memory before hashing it. A large object can exhaust the CLI process. | `cmd/upload/main.go:76`; `cmd/upload/main.go:159`; `cmd/sha256sum/main.go:48`; `cmd/sha256sum/main.go:62`; `cmd/sha256sum/main.go:111` | Source review confirmed the old paths used `os.ReadFile` and `io.ReadAll`. Upload hashes a 16 MiB file to the expected digest. `sha256sum` file and HTTP tests pass; a 4 MiB HTTP reader rejects read buffers over 64 KiB, and tests verify body closure and cancellation. CLI output code is unchanged. Focused package tests, `cmd` compile, vet, formatting, and diff check pass. The full root suite was attempted; unrelated existing listener tests failed because the sandbox denies loopback binds (`httptest` and endpoint port 9005). | Fixed |
| QA-018 | High | Deleting a physical bucket previously required permission on only one of its scopes, then deleted the shared credential and every scope. Deletion now requires access to every matching project scope. | `internal/buckets/commands.go:145`; `internal/buckets/service_test.go:260` | Regression failed before the fix and passes afterward; `go test ./internal/buckets -count=1` passes | Fixed |
| QA-019 | Medium | A bucket visibility query previously accepted an object through one authorized resource but returned all of that object's controlled resources. The query now projects only authorized associations. | `internal/persistence/store/visibility.go:11`; `internal/persistence/store/query_characterization_test.go:149` | SQLite regression failed before the fix and passes afterward; store and bucket package tests pass | Fixed |
| QA-020 | High | Without an explicit Azure endpoint, generated Blob SAS URLs use `<account>.blob.db.windows.net`. The standard Blob endpoint uses `blob.core.windows.net`, so these URLs fail for normal Azure accounts. Signing and deletion now use one resolved service URL with the standard default; explicit endpoints retain their existing normalization. | `internal/storage/azure/azure.go:232`; `internal/storage/azure/delete.go:22` | Regression verifies the generated SAS host, the default deletion host through a fake transport, and preservation of explicit endpoints. `go test ./internal/storage/azure ./internal/storage ./internal/transfers/... -count=1`, matching `go vet`, and `git diff --check` pass. No live provider-backed check was run. | Fixed |
| QA-021 | Medium | A transient provider error during multipart abort left the durable session claimed, so further abort attempts conflicted until the one-hour stale lease expired. Abort failures now release the token-matched claim to active state with a bounded context detached from caller cancellation. Successful provider aborts also finalize the session with bounded cleanup. | `internal/transfers/multipart.go:225`; `internal/transfers/multipart.go:268`; `internal/persistence/store/multipart.go:126` | The transfer regression cancels the provider context, confirms the session stays active and unclaimed, then retries immediately; a second test confirms provider success deletes the session even if the request context is canceled. SQLite verifies token-matched release, rejects a stale token, reclaims immediately, then deletes after success. Transfer, store, and SQLite package tests plus vet pass; PostgreSQL package tests pass, but database integration was skipped because `SYFON_TEST_POSTGRES_DSN` is unset. `cmd/server` compiles. | Fixed |
| QA-022 | Medium | GCS multipart part URLs used `Credential.Endpoint`, but completion and abort clients ignored it, sending operations to a different backend. The shared client factory now maps the configured root/prefix to the SDK API endpoint; blank endpoints retain emulator and production defaults. | `internal/storage/gcs/access.go:229`; `internal/storage/gcs/multipart.go:24`; `internal/storage/gcs/gcs_test.go:152` | Fake-transport regressions verify part URLs and multipart completion/abort requests use the configured host and API prefix even when the emulator host differs. Separate regressions retain `STORAGE_EMULATOR_HOST` and `storage.googleapis.com` defaults. GCS, storage, and transfer tests and vet pass; `cmd/server` compiles. This verifies client-side URL selection; live GCS or custom-endpoint acceptance was not tested. | Fixed |
| QA-023 | Medium | Most CLI commands load a profile's expired access token and send it as Bearer, even when its API key can refresh it. Those commands fail authentication until the token is refreshed elsewhere. Profile credentials are now validated and refreshed before client construction; the refreshed token is persisted and used by the client. | `cmd/cliauth/auth.go:37`; `cmd/cliauth/auth.go:66`; `cmd/cliauth/auth.go:114`; `cmd/projectcopy/auth.go:81`; `cmd/projectcopy/auth.go:88` | A temporary expired-token profile with a valid API key refreshes once, persists the new token, and a request through the constructed client sends it as Bearer; a valid token does not refresh. A valid token-only profile still loads. `go test ./cmd/cliauth -count=1`, focused `cmd/copyproject` tests, compile-only `cmd/copyproject` check, and vet pass. The complete `cmd/copyproject` suite was not run because its existing loopback listener tests are blocked in this sandbox. | Fixed |
| QA-024 | Medium | With multiple destination organization scopes and an untranslatable source project path, `copy-project` could pair one bucket with another bucket's organization path and attempt a write before discovering translation failed. Destination scope candidates are now selected in sorted bucket order with each bucket's organization and project scopes kept together; the project path is validated before any scope write. | `cmd/copyproject/scopes.go:36`; `cmd/copyproject/scopes.go:64`; `cmd/copyproject/scopes.go:130`; `cmd/copyproject/scopes.go:161` | Before the fix, focused regressions reproduced a `target-a`/`target-b` path mismatch and a scope write before an untranslatable-path result. Afterward, tests confirm stable bucket/path pairing and a validation error with zero `AddScope` calls. Focused `cmd/copyproject` regressions, selected existing copy-project tests, `go test ./cmd -run '^$' -count=1`, vet, gofmt, and diff-check pass. The full `cmd/copyproject` suite was not run because existing listener tests are blocked by the sandbox's loopback restriction. | Fixed |
| QA-025 | Medium | `syfon validate --addr` and `--debug` were rejected by Cobra because the command registered them with the standard flag package only after Cobra parsed its flags. Both flags are now registered with Cobra when the command is constructed. | `cmd/validate/main.go:16`; `cmd/validate/main.go:52`; `cmd/validate/validate_test.go:132` | Before the fix, the regression returned `unknown flag: --addr`. It now parses both flags and reaches the listener call using a valid temporary OpenAPI spec and invalid bind address. `go test ./cmd/validate -count=1`, compile-only `go test ./cmd -run '^$' -count=1`, vet, gofmt, and diff-check pass. | Fixed |
| QA-026 | Medium | If the validator cannot bind its listener, it previously logged the error and exited successfully. Listener and OpenAPI setup errors now return from Cobra's `RunE`, so the root command exits nonzero on failure. | `cmd/validate/main.go:20`; `cmd/validate/main.go:40`; `cmd/validate/main.go:49`; `main.go:11` | The same regression uses an invalid bind address, confirms `Execute` returns the listener error, and does not open a socket. Focused validate tests, the root command compile check, vet, gofmt, and diff-check pass. The full `cmd` suite was not run due the known sandbox loopback restriction. | Fixed |
| QA-027 | Low | `syfon metrics transfers users --limit` set `total_users` to the number of returned rows. It now counts all matching user breakdown rows before applying the client-side limit, while the `users` array remains sorted and limited. | `cmd/metrics/main.go:135`; `cmd/metrics/main.go:209`; `cmd/metrics/main.go:211` | Before the fix, the three-user limit-one regression reported one user and `total_users=1`. It now reports one user with `total_users=3`; without a limit it reports three and three. The test also checks CLI help and the contract is documented in `docs/access-grants-transfer-metrics.md`. `go test ./cmd/metrics -count=1`, compile-only `go test ./cmd -run '^$' -count=1`, vet, gofmt, and diff-check pass. | Fixed |
| QA-028 | Low | An S3 multipart part number above 10,000 received a presigned upload URL even though S3 rejects that number. The transfer service now rejects it before provider dispatch, and the S3 adapter validates 1–10,000 before client lookup or presigning. Other providers retain their own upper-bound rules. | `internal/transfers/multipart.go`; `internal/storage/s3/multipart.go`; `internal/storage/types.go` | Before the fix, focused regressions showed part 10,001 reached the transfer provider, and S3 direct calls accepted 0 and 10,001. After the fix, tests verify 1 and 10,000 pass, invalid S3 values stop before provider/presigner calls, and a non-S3 10,001 passes through. `go test ./internal/transfers ./internal/storage/s3 ./internal/storage -count=1` and matching vet pass; transfer LFS and HTTP API suites pass. `cmd/server` tests passed on the authorized local-listener rerun after the sandbox denied loopback. | Fixed |
| QA-029 | Medium | A local `file:` upload copied to EOF without checking the declared size. If the source length changed after the uploader stats it, a shorter or longer file could be accepted while the client registered the earlier size and checksum. Local uploads now reject short and long bodies before replacing the destination. | `client/transfer/http.go:72`; `client/transfer/http.go:78`; `client/transfer/http.go:83` | Regressions confirm short input, long input, and non-empty data declared as zero all fail while preserving existing destination bytes. Exact-size and empty-file uploads pass. `go test ./client/transfer/... -count=1`, vet, and direct-importer compile checks pass. The repository-wide test run is blocked by sandbox-denied loopback listeners in unrelated tests. | Fixed |
| QA-030 | None | A canceled service context can cause multipart-session persistence to fail and its cleanup abort to receive the same canceled context. Current production HTTP handlers pass Fiber contexts that do not cancel on client disconnect, so no reachable production trigger was established. | `internal/transfers/multipart.go:74`; `internal/transfers/multipart.go:82`; `internal/httpapi/transfers_routes.go:81` | Service-level probe reproduced the sequence; caller and Fiber context review falsified the stated HTTP trigger | Rejected |
| QA-031 | Medium | S3 object keys containing URL syntax such as `?`, `#`, or percent escapes did not round-trip through canonical URL construction and parsing. Signed-upload conversion and storage target serialization could point to a truncated or changed key. All canonical S3 URL branches now use `url.URL` serialization through `BucketToURL`, and parsing retains the decoded key bytes. | `client/services/storage_url.go`; `internal/storage/address/path.go`; `internal/transfers/target.go` | Before the fix, regressions showed raw delimiters/percent sequences in generated URLs and the final resolved key changing or failing to parse. After the fix, tests distinguish an escaped slash (`%2F` → key `/`) from literal `%2F` (`%252F` → key `%2F`), preserve `?`, `#`, and `%`, keep ordinary URLs unchanged, and verify the resolved storage signing target has the exact key. Focused tests and vet pass; full tests pass for client/services, address, transfers, projects/storage, persistence/store, transfers/lfs, and buckets. The `client/services` full suite required an authorized local-listener rerun after sandbox loopback denial. Its shared fixture now returns 200 for an un-ranged `GetReader`; no production download behavior changed. | Fixed |
| QA-032 | Medium | `syfon upload` hashes a file, then reopens it for upload. `RegisterFile` now streams SHA-256 over the opened source before URL resolution or upload and rejects invalid or mismatched digests, so same-size replacements already present when registration starts cannot be published under the stale key. The upload engine still reopens the path, so mutation after this preflight check remains possible. | `cmd/upload/main.go:75`; `client/transfer/upload/orchestrator.go:108`; `client/transfer/engine/uploader.go:151` | The same-size replacement regression failed before the fix and now fails before URL resolution, storage upload, or metadata registration; unchanged content succeeds. Focused registration tests, fresh `client/transfer/upload` and `cmd/upload` tests, full `cmd/copyproject` tests, and vet for all three packages pass. The full repository suite passes after QA-059 was also fixed. This is a streaming preflight check, not an immutable snapshot. | Fixed |
| QA-033 | High | With SQLite's default foreign-key enforcement, migration of a legacy `drs_object` table with `file_name` could drop child rows while replacing the parent. Migration now disables foreign keys on its dedicated connection, rebuilds the table in a transaction, checks relationships before commit, then restores enforcement. | `internal/persistence/sqlite/dialect.go`; `internal/persistence/sqlite/sqlite_test.go` | Regression fixture lost seven FK-backed child sets before the fix. Afterward, all parent and child rows survive with an empty `foreign_key_check`; a conflicting staging table makes migration fail while leaving the original database queryable. SQLite, PostgreSQL, store, LFS, objects, and HTTP API package tests plus vet pass | Fixed |
| QA-034 | Medium | A documented 32-byte raw credential master key made entirely of Base64 characters is decoded first and rejected as 24 bytes, preventing configured-bucket startup and later credential encryption. The parser now falls back to the raw bytes when Base64 decoding does not produce a 32-byte key. | `internal/persistence/credentialcipher/local_key_manager.go:29`; `docs/encryption.md:45`; `cmd/server/bootstrap.go` | The raw-key encryption regression failed before the fix and passes after it; configured-bucket startup persists credentials with the raw key. Hex and Base64 key cases pass, and invalid key lengths remain rejected. Credentialcipher, SQLite, PostgreSQL, objects, and focused server tests plus vet pass. | Fixed |
| QA-035 | Medium | For records with at most one controlled resource, `syfon rm` asked to delete storage data. This server advertises that operation as unsupported and rejects the request before deleting metadata, so the command failed for that branch. | `cmd/rm/main.go:37`; `cmd/server/server.go:52`; `internal/httpapi/drs_routes.go:157` | The command now sends `delete_storage_data:false` and reports that storage data was preserved; its regression asserts the request and output. `TestDeleteAndAccessMethodRoutes` passes, covering successful metadata deletion and rejection of storage deletion with the record unchanged. The `cmd/rm` regression could not run because the sandbox denied `httptest.NewServer` loopback binding; command compile, vet, formatting, and diff-check pass. | Fixed |
| QA-036 | Medium | Bulk DRS deletion skipped missing or unauthorized IDs, deleted the permitted subset, and returned 204. The object service now rejects any missing or unauthorized member before mutation, and the store rechecks existence and delete authority in its transaction before removing the full set. | `internal/objects/deletion.go:99`; `internal/persistence/store/mutations.go:305`; `internal/httpapi/drs_routes.go:207`; `apigen/openapi/openapi.yaml:1126` | Before the fix, mixed missing and unauthorized requests returned 204 and deleted the authorized object. They now return 404 or 403 with both records preserved; a valid two-object request returns 204 and deletes both. The SQLite transaction regression rejects a missing member without deleting its valid member. Objects, store, SQLite, PostgreSQL, and HTTP API tests plus vet pass. The PostgreSQL deletion integration test is skipped because `SYFON_TEST_POSTGRES_DSN` is unset. | Fixed |
| QA-037 | Medium | The bulk SHA-256 validity request schema accepts `hashes`, but its handler required `sha256`. A client using the documented `hashes` field received 400. The handler now accepts either field, accepts both only when their arrays are identical, and rejects missing, empty, or whitespace-only values with 400. | `apigen/openapi/internal.openapi.yaml:513`; `internal/httpapi/records_routes.go:121`; `internal/httpapi/records_routes_test.go:274` | Generated-client route tests return the same validity map through `sha256`, `hashes`, or identical dual fields; empty and conflicting requests return 400. The regression failed before the fix because `hashes` alone returned 400 and conflicting fields returned 200. HTTP API tests, vet, OpenAPI YAML parse, gofmt, and diff-check pass. | Fixed |
| QA-038 | Medium | Authentication plugin RPC could not carry ordinary nested claim or metadata values inside `map[string]interface{}` without gob registration. A structured claim could fail RPC encoding and deny authentication or authorization. The protocol now uses JSON byte fields and bundled adapters. | `plugin/types.go`; `internal/access/authentication/plugin_clients.go`; `plugin/local_auth/main.go`; `plugin/gen3_auth/main.go`; `docs/plugins.md` | Regression failed before the fix; nested maps and arrays round-trip through the production RPC adapters over `net.Pipe`; malformed and non-object JSON is rejected; focused test, plugin package tests, and vet pass. Full authentication package is blocked by the sandbox's loopback listener restriction in an unrelated token test | Fixed |
| QA-039 | Medium | Built-in Gen3 authentication previously accepted an invalid Fence URL and then rejected every token issuer. Config loading now resolves environment values before validation and requires an HTTPS Fence URL with a host for the built-in verifier; custom auth plugins and mock auth do not require Fence. | `internal/config/load.go:33`; `internal/config/validation.go:221`; `cmd/server/helm_config_test.go:15` | Regression rejected an invalid effective environment URL; config suite passes, and the Helm config test, `helm lint`, and rendered Gen3 config pass with the chart's `https://<hostname>/user` default | Fixed |
| QA-040 | Medium | A canonical record associated with multiple project scopes could resolve its access URL to a bucket/key that belongs to none of them. Target selection took the last bucket but concatenated prefixes from every scope, so a signed download could target the wrong object. | `internal/transfers/target.go:93`; `internal/transfers/service_test.go:294`; `internal/transfers/service_test.go:328` | Regression selects the project from a sibling prefix and checks the composed bucket/key; an end-to-end `IssueAccess` test signs the selected project URL; ambiguous scope is rejected; existing nested-prefix and traversal tests pass; transfer, LFS, and HTTP API suites plus vet pass | Fixed |
| QA-041 | Medium | Scoped project repair previously could not find a damaged or imported deterministic-ID record whose controlled-access row was missing. Repair now discovers records under the configured S3 bucket/prefix, then requires a valid SHA-256 and an ID derived from the requested project before reporting or repairing the association. | `internal/projects/storage/repair.go:167`; `internal/objects/listing.go:48`; `internal/objects/repair.go:20`; `internal/persistence/store/object_reads.go:94`; `internal/persistence/store/mutations.go:450` | Authorized SQLite regression confirms the normal scoped list returns zero before repair; audit reports only the matching deterministic ID, apply restores its access row, and an unrelated in-prefix record remains untouched. Projects/storage, objects, store, and SQLite tests plus vet pass | Fixed |
| QA-042 | Medium | Scoped usage metrics previously handled aliases inconsistently: bulk metrics omitted them, while single-file metrics authorized the alias and then queried usage under that alias ID. Metrics now resolve requested physical IDs and aliases in one batch before scope checks and use the canonical ID for usage lookup; the API schema documents alias acceptance and canonical response IDs. | `internal/usage/service.go:123`; `internal/objects/queries.go:96`; `internal/persistence/store/object_reads.go:35`; `apigen/openapi/metrics.openapi.yaml:120`; `apigen/openapi/metrics.openapi.yaml:488` | SQLite regression resolves physical IDs and aliases in one batch; usage regressions verify scoped single and bulk lookups return canonical usage and reject an alias outside scope. Usage, objects, store, SQLite, and HTTP API tests plus vet pass | Fixed |
| QA-043 | Medium | Canonical duplicate repair previously discarded the removed record's durable usage counters and pending events. Repair now adds its counters to the survivor, reassigns pending events, and flushes them in the same transaction before commit. PostgreSQL serializes event writes against repair per object, and writes using a repaired alias resolve to the survivor. | `internal/persistence/store/object_writes.go`; `internal/persistence/store/usage.go`; `internal/persistence/postgres/dialect.go`; `internal/persistence/sqlite/canonical_repair_test.go`; `internal/persistence/postgres/usage_flush_integration_test.go` | The SQLite regression failed before the fix (3 uploads instead of 7) and now verifies merged totals, latest event timestamps, no pending events, alias writes, and unchanged totals after retry. Store, SQLite, PostgreSQL, objects, storage, usage, HTTP API, and server compile checks plus vet pass. PostgreSQL concurrency regression added but skipped locally because `SYFON_TEST_POSTGRES_DSN` is unset. | Fixed |
| QA-044 | Medium | Production config accepted `DRS_CREDENTIAL_KMS_KEY_ID` as proof of stable encryption even when explicit `DRS_CREDENTIAL_KEY_MANAGER=local` made the cipher ignore KMS and use a local key. If that key was not persisted, database-only bucket credentials became unreadable after replacement. Validation now follows the cipher's manager precedence. | `internal/config/validation.go:244`; `internal/persistence/credentialcipher/config.go:54`; `cmd/server/bootstrap.go:386` | The regression failed before the fix for explicit local with only a KMS ID, explicit KMS without an ID, and an unknown manager with an ID. The matrix now accepts configured or environment local keys, explicit KMS with an ID, and default KMS selection from an ID. The config suite, server vet, configured Helm credential test, `helm lint /Users/peterkor/Desktop/gen3-helm/helm/syfon`, formatting, and diff-check pass. The chart remained unmodified. | Fixed |
| QA-045 | Medium | LFS metadata staging ignored `ttl_seconds` and used a fixed 20-minute candidate deadline. A valid upload completing after that deadline could keep its receipt while Verify rejected the expired candidate. The HTTP boundary now applies a documented 20-minute default and accepts 1 through 86400 seconds; receipt persistence extends both database and embedded candidate expiry so Verify can use the candidate for the receipt window. | `apigen/openapi/lfs.openapi.yaml:440`; `internal/httpapi/lfs_routes.go:409`; `internal/transfers/lfs/service.go:484`; `internal/persistence/store/lfs.go:138` | The 45-minute request regression now stores the requested TTL; default TTL remains 20 minutes; zero, negative, and above-24-hour values return 400. SQLite regressions prove an expired staged candidate becomes usable when its valid receipt is saved, Verify registers it, and an expired candidate without a receipt remains unavailable. Focused HTTP/LFS/persistence suites, vet, formatting, diff-check, and the full repository suite pass. The targeted API-generator run could not download its Go 1.25 toolchain; the OpenAPI source parses and the existing generated request model already includes `TtlSeconds`. | Fixed |
| QA-046 | Medium | When `copy-project` creates a destination scope on a different storage provider, it retains the source or default URL scheme in the copied record's access metadata. The bytes upload to the target provider, but the published access method can point to the wrong provider. | `cmd/copyproject/scopes.go:88`; `cmd/copyproject/copy_record.go:108`; `cmd/copyproject/storage_paths.go:122`; `client/transfer/upload/orchestrator.go:44` | The pre-fix URL regression returned `s3://` for a GCS destination; provider resolution now covers GCS and Azure, and absent metadata keeps the S3 default. Focused tests verify GCS/Azure/S3 schemes and destination bytes plus GCS DRS metadata. The command-level HTTP regression confirms the serialized access method is `gs://`; the full root and client suites, vet, gofmt, and diff-check pass. The suites required loopback access. | Fixed |
| QA-047 | Medium | `copy-project` can upload a legacy record's bytes before the target rejects registration for a missing valid SHA-256. The failed copy leaves an unregistered remote object; retries with an absent SHA use new temporary names and can leave more. | `cmd/copyproject/copy_record.go:81`; `client/transfer/upload/orchestrator.go:107`; `internal/objects/record.go:285` | Before the fix, the missing-SHA regression uploaded under a random temp filename and had no digest to register. Copy-project now hashes the downloaded file before target calls, uses that digest for its upload key and registration, and rejects invalid/conflicting digests before URL resolution or upload. The targeted regressions pass; the full root and client suites plus vet, gofmt, and diff-check pass. | Fixed |
| QA-048 | Medium | `copy-project` drops a source record's description when constructing the target DRS object. Successful copies can lose that user metadata even though the target registration path can preserve it. | `cmd/projectcopy/record_selection.go:85`; `cmd/copyproject/copy_record.go:99`; `client/transfer/upload/orchestrator.go:219` | The pre-fix regression showed a non-empty source description reached `copyRecord` but the target DRS candidate had no description. The candidate now preserves non-empty, nil, and empty values. Focused description and copy-project regressions pass; the full root and client suites, vet, gofmt, and diff-check pass. | Fixed |
| QA-049 | Medium | Registration, replacement, and explicit alias creation now reject an alias ID already used by a different physical record. The shared transaction check prevents direct reads and alias resolution from diverging or an old physical ID from later resolving to an unrelated object. | `internal/persistence/store/object_writes.go`; `internal/persistence/store/mutations.go`; `internal/persistence/sqlite/content_identity_test.go`; `internal/persistence/postgres/content_identity_integration_test.go` | SQLite regression failed before the fix (registration returned nil); it now checks all three write paths reject atomically, preserve physical reads, and do not reassign the ID after its physical record is deleted. PostgreSQL equivalent added; it compiles but is skipped locally without `SYFON_TEST_POSTGRES_DSN`. Store, SQLite, PostgreSQL, objects, storage, and HTTP API tests plus vet pass | Fixed |
| QA-050 | Medium | A permanent download HTTP error such as 403 was retried five times because the downloader passed typed API errors directly to `RetryAction`. The downloader now marks permanent HTTP statuses terminal for both full and ranged reads while leaving transport errors and 408, 429, and 5xx statuses retryable. | `client/transfer/engine/downloader.go:286`; `client/transfer/engine/downloader.go:366`; `client/transfer/engine/downloader_retry_test.go:274` | Before the fix, the typed 403 regression made six reader calls and five backoff waits. It now makes one call and no wait. Typed 408, 429, and 503 each retry once and succeed. Engine, transfer/download, transfer/upload, client/transfer, cmd/download, and focused client/services tests pass; vet passes for affected packages. The full repository suite passes with loopback access enabled; ordinary sandbox runs of `client/services` and `cmd/copyproject` hit listener restrictions. | Fixed |
| QA-051 | Medium | After one multipart upload part failed terminally, other workers could start queued part requests before the uploader returned the error. The part workers now share a child context; the first terminal error cancels the worker pool, which checks cancellation before another provider request. | `client/transfer/engine/uploader.go:306`; `client/transfer/engine/uploader.go:382`; `client/transfer/engine/uploader_queue_test.go:107` | Before the fix, the coordinated test started ten parts and then observed queued part 11 after part 1 failed. Afterward only the ten active parts start; the original terminal error returns, nine completed parts remain in the checkpoint with the same upload ID, and explicit resume uploads parts 1 and 11 without reinitializing. Focused engine/importer tests, race test, and vet pass; the root-module full suite and separate client-module full suite pass with loopback access enabled. | Fixed |
| QA-052 | Medium | DRS registration previously returned 201 for candidates with `contents` or `mime_type` even though persistence discarded both. Registration now rejects either field, and the first-party upload client rejects them before uploading bytes. | `internal/objects/record.go:285`; `client/transfer/upload/orchestrator.go:52`; `apigen/openapi/openapi.yaml:890`; `apigen/openapi/syfon-error.yq`; `internal/httpapi/drs_routes_test.go:147`; `client/transfer/upload/orchestrator_test.go:166` | Generated-client route regressions failed before the fix with 201 for both fields; they now get 400 naming the field and a follow-up GET returns 404. Ordinary blob registration returns 201 and reads back with 200. Upload-client regressions failed before the fix because both fields were accepted; they now return `ErrInvalidInput` before storage resolution, upload, or registration. Focused object, HTTP API, upload-client, OpenAPI, and codegen tests plus vet pass. PyYAML parsed the spec and local `yq` applied the custom overlay. `oapi-codegen` generation could not run because local Go 1.22.10 cannot build the generator requiring Go 1.25.0 and the toolchain download is unavailable offline | Fixed |
| QA-053 | Medium | Raw storage URL inspection previously relied on bucket visibility alone, allowing a Gen3 caller to probe keys outside authorized project prefixes in a shared bucket. Single, bulk, and bulk-list inspection now check the caller's authorized bucket scopes before provider access. | `internal/projects/storage/probe.go:164`; `internal/projects/storage/list_validation.go:207`; `internal/projects/storage/service_test.go:430` | No-network regressions cover single, bulk, and bulk-list inspection; `go test ./internal/projects/storage -count=1` passes | Fixed |
| QA-054 | High | Token refresh previously followed a cross-origin HTTP 307/308 redirect with the API key in its request body. Refresh now stops before making a request to a different origin, while same-origin redirects remain supported. | `client/request/auth.go:75`; `client/request/auth_test.go:501` | Synthetic no-network regression failed before the fix and passes after it; focused request/client tests and vet pass | Fixed |
| QA-055 | Medium | Token refresh previously ignored the configured HTTP client timeout. The refresh client now uses that timeout and preserves the configured redirect policy. | `client/request/auth.go:74`; `client/request/transport.go:185`; `client/request/auth_test.go:607` | Synthetic timeout regression failed before the fix and passes after it; focused request/client tests and vet pass | Fixed |
| QA-056 | Medium | Internal delete-by-query previously ignored a supplied checksum filter and could remove the project's access grant from unrelated records. The route now uses checksum-filtered deletion when `hash` is present and rejects `hash_type` without a hash. | `internal/httpapi/records_routes.go`; `internal/objects/deletion.go`; `internal/httpapi/records_routes_test.go` | Route regression confirms only the matching checksum is removed; object regression confirms other project references remain. Focused HTTP API and object tests pass | Fixed |
| QA-057 | Medium | A determinate multipart completion error could leave its durable claim stuck when the request context was canceled. `ReleaseMultipartCompletion` used that canceled context, and the service discarded its error, so an identical retry conflicted until the lease expired. Completion cleanup now uses a bounded cancellation-detached context, reports release failures, preserves claims for indeterminate provider outcomes, and finalizes confirmed success after caller cancellation. | `internal/transfers/multipart.go:173`; `internal/transfers/multipart.go:179` | The regression fails before the fix with the session still `completing/complete`, then confirms canceled determinate failure returns the session to active and allows immediate retry. A second regression confirms successful provider completion is persisted after request cancellation, while the indeterminate-outcome test still retains its claim. Transfer, store, and SQLite package tests plus vet pass; PostgreSQL package tests pass, but database integration was skipped because `SYFON_TEST_POSTGRES_DSN` is unset. `cmd/server` compiles. | Fixed |
| QA-058 | Medium | When a GCS bucket's credential identity changed, the client cache retired its old entry but then reloaded the same client from a secondary `sync.Map`, pairing old endpoint/credentials with the new identity. Cache invalidation now deletes that stale entry before creating a replacement. | `internal/storage/gcs/multipart.go:201`; `internal/storage/gcs/gcs_test.go:544` | Endpoint-change regression reproduced stale-client reuse before the fix and passes after invalidation; GCS, storage, and transfer tests and vet pass. This verifies local cache identity behavior; live GCS or custom-endpoint acceptance was not tested. | Fixed |
| QA-059 | Medium | After `copy-project` added a destination project scope, reads of an object still stored at a legacy bucket-wide key could fail as ambiguous when the object was authorized in both the source and destination scopes. Scope resolution now uses a bucket-wide candidate when no more-specific prefix matches, while prefixed candidates still take precedence. | `internal/transfers/target.go:373`; `internal/transfers/service_test.go:377`; `cmd/cli_basic_commands_test.go:545` | Before the fix, a regression with a bucket-wide source scope and a sibling prefixed project scope returned `ErrInvalidInput: ambiguous storage scope`. Afterward that target resolves to the bucket-wide key, the existing cross-bucket ambiguous-scope test still rejects, both failing copy-project integration tests pass, and the full repository suite passes. | Fixed |
| QA-060 | Medium | `copy-project` rejected a valid source project scope whose path was the bucket root (for example, `file://syfon-bucket`) because there was no project suffix to translate. The command now uses its destination project default path for bucket-root source scopes and still rejects unrelated non-root paths. | `cmd/copyproject/scopes.go:151`; `cmd/copyproject/storage_paths.go:103`; `cmd/cli_basic_commands_test.go:368` | GitHub CI reproduced the failure in the copy-project command tests on Linux. Bucket-root path cases are covered by `TestStoragePathHelpers`; with loopback access enabled, `go test ./cmd ./cmd/copyproject ./cmd/server ./internal/projects/storage -count=1` passes. The latest full CI run is pending. | Fixed |

## Audit method

New findings need a concrete trigger, a wrong observable result, a current source path, and a check of relevant callers or tests. Record a reproduction when one is practical. Keep speculative risks and code cleanup out of the open bug count. Preserve compatibility with `/Users/peterkor/Desktop/gen3-helm/helm/syfon` when assessing server bootstrap changes.

Five read-only discovery waves covered client requests and transfers, CLI commands, HTTP and object services, bucket/project scopes, storage providers, SQLite/PostgreSQL persistence, server configuration, Helm integration, LFS, and multipart transfers. Fresh reviewers challenged each new wave and then rechecked every ledger row against the current working tree. Focused package tests and no-network probes passed where listed. Tests that bind a loopback listener could not run in this sandbox, and live PostgreSQL/provider integration was unavailable. Helm rendering and focused config tests were checked against `/Users/peterkor/Desktop/gen3-helm/helm/syfon`. The ledger records observed and source-confirmed findings, not a proof that every possible defect has been found.

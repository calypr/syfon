[build]
  command = "python3 -m pip install --upgrade pip && python3 -m pip install -r requirements-docs.txt && mkdocs build --strict"
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.12"
